@model ALC.WebApp.MVC.Models.ErrorViewModel
@{
    ViewData["Title"] = @Model.Title;
}
<div class="container">
    <div style="padding-top: 200px; text-align: center">
        @{
            if (Model == null)
            {
            <h2>Ooops! A error occured, but don't worry. Our team will be notified and we will fix it soon!</h2>
            }
            else
            {
            <h2>@Html.Raw(Model.Message)</h2>
            }
        }
    </div>
</div>